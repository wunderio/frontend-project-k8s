
# Values in this file override the default values of our helm chart.
#
# See https://github.com/wunderio/charts/blob/master/frontend/values.yaml
# for all possible options.

services: 
  hello:
    exposedRoute: '/hello'
    port: 4000
    env:
      FOO: 'BAR'

  world:
    exposedRoute: '/world'
    port: 5000
    replicas: 2
    mounts:
      - files
    cron:
      world-cli:
        command: echo $(date) > /app/files/lastupdate.txt
        schedule: "~ * * * *"

domainPrefixes: ['fi', 'lv' ]

mounts:
  files:
    enabled: true
    storage: 1G
    mountPath: /app/files
    storageClassName: silta-shared
    csiDriverName: csi-rclone

elasticsearch:
  enabled: true

rabbitmq:
  enabled: true
  rabbitmq:
    password: this-is-not-safe

shell:
  enabled: true

nginx:
  redirects:
    - description: 'Redirect main path to default service'
      from: '~://feature-redirects.frontend-project-k8s.silta.wdr.io'
      to: https://feature-redirects.frontend-project-k8s.silta.wdr.io/hello
